# Raíz del monorepo — la configuración real vive en cada app.
# Backend: copia apps/backend/.env.example a apps/backend/.env y complétalo.
# El frontend no necesita .env: habla con el backend por rutas relativas (/api).
